<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TextToImage" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TextToImage" parent="Base.Theme.TextToImage" />

    <style name="CustomMaterialButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/bg_buttons</item>
    </style>

    <style name="Theme.BottomNavScanApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#362990</item>
        <item name="colorPrimaryVariant">#651FFF</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="android:colorForeground">@color/white</item>
        <item name="statusBarForeground">#FFFFFF</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">
            true
        </item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/maincolorbg</item>
    </style>
    <style name="ContentLoadingProgress">
        <item name="colorAccent">@color/app_color</item>
    </style>


</resources>